import 'package:flutter/material.dart';

// import './view/placeholder/menu_bar.dart';
import './view/authentication/authenticate.dart';

class Wrapper extends StatelessWidget {
  @override
  Widget build (BuildContext context) {
    return Authenticate();
  }
}